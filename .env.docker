# Docker Environment Configuration for Graphiti MCP Server
# This file shows the environment setup for Docker deployment
# DO NOT commit this file if it contains real API keys

# NOTE: The Docker container will mount and read from:
# 1. .env.graphiti (for Graphiti/FalkorDB configuration)
# 2. ~/.env (for OPENAI_API_KEY)

# The wrapper script automatically handles:
# - Loading both .env files in correct order
# - Updating FALKORDB_HOST to host.docker.internal when in Docker
# - Configuring Python for unbuffered stdio output

# Example consolidated configuration (for reference):
# ===================================================

# From .env.graphiti:
GRAPHITI_GROUP_ID=shared_gtd_knowledge
FALKORDB_HOST=host.docker.internal  # Updated for Docker networking
FALKORDB_PORT=6380
FALKORDB_DATABASE=shared_knowledge_graph
MEMORY_DECAY_FACTOR=0.95
MEMORY_INCLUDE_HISTORICAL=false
ENABLE_GTD_INTEGRATION=true
ENABLE_CROSS_REFERENCES=true

# From ~/.env:
# OPENAI_API_KEY=your-actual-api-key-here
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Docker-specific settings:
PYTHONUNBUFFERED=1  # Essential for stdio communication
DOCKER_CONTAINER=true  # Flag to detect Docker environment

# Note: This file is for documentation purposes
# The actual values are loaded from mounted .env files at runtime